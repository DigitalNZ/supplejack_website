%body
  %button.filter-btn.menu Filters
  = render 'active_filters'
  .filter-container.menu
    %header
      %ul.tabs{"data-tab" => ""}
        %li.tab-title.active
          %a{:href => "#panel1-1"} Quick filters
        = render 'filter_tabs'
      %button.close
        ×
    .tabs-content
      = render 'quick_filters'
      = render 'filter_content'
